package CoucheAcces_Fichier;
/**
 * Enum ColumnType that represents the different types of columns
 */
public enum ColumnType { // for the different types of columns
    INT,
    FLOAT,
    STRING,
    VARSTRING
}
